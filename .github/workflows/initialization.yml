name: Clone necessary cloud templates

on:
  # push:
  #   branches: main
  workflow_dispatch:

jobs:
  comment:
    runs-on: ubuntu-latest
    name: "Clone cloud-deployment and cloud-infrastructure repos."
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/actions/initialization
        name: Initialize
        id: initialization
        with:
          CLIENT_REPO_PAT: ${{ secrets.CLIENT_REPO_PAT }}
          DASHG_CLOUD_INFRA_PAT: ${{ secrets.DASHG_CLOUD_INFRA_PAT }}
